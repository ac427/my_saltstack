{% set serial = pillar.get('serial', {}) -%}

$TTL 1W
@               IN SOA          localhost.   root.localhost. (
                                1              ; {{ serial }}
                                2D              ; refresh
                                4H              ; retry
                                6W              ; expiry
                                1W )            ; minimum

                IN NS           localhost.
1               IN PTR          localhost.

